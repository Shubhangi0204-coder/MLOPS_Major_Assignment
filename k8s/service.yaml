apiVersion: v1
kind: Service
metadata:
  name: olivetti-service
spec:
  selector:
    app: olivetti
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080    # optional explicit NodePort in 30000-32767
  type: NodePort

